# Build Check

Recommended command:

```bash
mvn -q -DskipTests package
```

If Maven is not installed in the environment, run the lightweight structural check:

```bash
bash scripts/validate-structure.sh
```

This repository is generated for Java 21, Spring Boot 4.1.x and Spring AI 2.0.x.
